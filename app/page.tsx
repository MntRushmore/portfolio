import BootSequence from "@/components/boot-sequence"

export default function Home() {
  return (
    <main className="min-h-screen bg-black overflow-hidden font-['VT323',monospace] text-white">
      <BootSequence />
    </main>
  )
}
